// Call to Action — No JS required. All styles handled by CSS.
(function () {
    'use strict';
    // Reserved for future interactivity (e.g. analytics events, scroll animations).
    function initCallToAction() {
        // no-op
    }

    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initCallToAction);
    } else {
        initCallToAction();
    }
})();
